// Single source of truth for the site's canonical URL -- used by the
// sitemap, robots.txt, and page metadata. Swapping to a real domain later
// (once Ben buys one on Namecheap) is a one-line change here, not a rebuild.
export const SITE_URL = 'https://canopymarket.store';
